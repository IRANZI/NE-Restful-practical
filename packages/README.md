# Packages

This folder is reserved for shared workspace packages. The current services are intentionally self-contained so each microservice can run independently with its own `src`, `migrations`, `Dockerfile`, `package.json`, and `tsconfig.json`.
